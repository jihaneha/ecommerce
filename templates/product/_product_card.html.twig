{# affichage d'un produit #}
<div class="card">
	<div class="row mt-3">
		<div class="text-center" id="img">
			<img src="/images/{{p.mainpicture}}" alt="image du produit {{ p.name }}" class="img-fluid">
			<div class="card-body">
				<p class="card-little">{{ p.name }}
					({{ p.price / 100}}€)</p>
				<a href="{{ path('product_show',{'category_slug':p.category.slug,'slug':p.slug}) }}" class="btn btn-secondary btn-sm mb-2 mt-2 ">Détails</a>
				<a href="{{path('cart_add', {'id': p.id }) }}" class="btn btn-dark btn-sm">Ajouter</a>
			</div>
		</div>
	</div>
</div>
